# Tasks

## Done

- Create Rust repository scaffold.
- Review `sdague/amt` PR #19 and PR #29.
- Remove VNC/KVM from the target feature set.
- Port the simple non-VNC AMT command surface to Rust.
- Document the accepted and rejected PR #19/#29 changes.
- Document current AMT protocol compatibility scope.
- Document possible roadmap additions.
- Add Makefile `compile` and `install-bin` targets.
- Add `password_ref` gopass credential resolution.
- Add repo-local `.config/amt/hosts.toml` support.

## Next

- Re-enable `install pxe` only after hardware validation.
- Add integration tests with recorded AMT responses.
- Test against a real modern AMT host.
- Create and push `marcelofpfelix/amt` on GitHub after explicit approval.
